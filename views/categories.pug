extends layout

block content
  h1= title
  each val, index in data
    div
      h4
        a(href=`/categories/${val.url}`)= val.title
      div
        img(src=`/images/categories/${val.image}`)
      p= val.description
